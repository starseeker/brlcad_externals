if (MSVC)
	execute_process(COMMAND "@VCVARS_BAT@" && cmake --install . WORKING_DIRECTORY @QT_BUILD_DIR@)
else (MSVC)
	execute_process(COMMAND cmake --install . WORKING_DIRECTORY @QT_BUILD_DIR@)
endif (MSVC)

# Local Variables:
# tab-width: 8
# mode: cmake
# indent-tabs-mode: t
# End:
# ex: shiftwidth=2 tabstop=8

